"use strict";

// Retained as a compatibility tombstone for older copies of index.html.
// The player no longer applies an iframe sandbox because some providers detect it.
// Current builds use ad-blocker-help.js and installed browser content blockers instead.
